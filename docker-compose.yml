version: '3.11'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    networks:
      - network
    environment:
      - PYTHONUNBUFFERED=1

networks:
  network:
    name: postgres_network
    external: true
